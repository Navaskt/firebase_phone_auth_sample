name: firebase_phone_auth_sample
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
# Optional, from Flutter template
  cupertino_icons: ^1.0.8

# Firebase
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  image_picker: ^1.2.0
  cloud_firestore: ^6.0.2
  firebase_storage: ^13.0.2
  country_code_picker: ^3.0.0
  sms_autofill: ^2.3.0
  intl: ^0.20.2
  connectivity_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
